import React from 'react';

const NotFoundPage = () => (
  <div
    css={{
      marginTop: '2rem',
    }}
  >
    <h1>NOTHING FOUND</h1>
    <p>Pretend that the page exists for now.</p>
  </div>
);

export default NotFoundPage;
